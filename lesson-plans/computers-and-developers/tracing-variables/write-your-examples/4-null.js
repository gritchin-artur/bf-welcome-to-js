'use strict';

/* Null

TODO: create a few variables(null):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

let availableProducts = null; // declare and initialization let availableProducts

let money = null; // declare and initialization let money

let status = `Your money: ${money} \n Available products: ${availableProducts}`; // declare and initialization let status

console.log(status); // read let status

availableProducts = 100; // reassign let availableProducts

money = 45; // reassign let money

status = `Your money: ${money} \n Available products: ${availableProducts}`; //reassign let status

console.log(status); // read let status
