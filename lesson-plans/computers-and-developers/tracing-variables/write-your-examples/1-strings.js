'use strict';

/* Strings

TODO: create a few variables(strings):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

let car = 'volkswagen'; // declare and initialization let car

let color = 'white'; // declare and initialization let color

let myCar = `Mycar is ${color} ${car}`; // declare and initialization let myCar

console.log(myCar); //read myCar

car = 'audi'; // reassign variable car

color = 'black'; // reassign variable color

myCar = `I changed my car to ${color} ${car}`; // reassign variable myCar

console.log(myCar); //read myCar
